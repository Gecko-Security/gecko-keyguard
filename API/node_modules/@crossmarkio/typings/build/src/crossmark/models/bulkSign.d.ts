import { BaseFullResponse, BaseRequest, BaseResponse, ExtendedSignOpts } from './common/base';
import { IndexedTransactionRequest, AllTransactionResponse } from './common/tx';
import { Status } from './common/status';
import { Extension } from '../../extension/index';
export interface BulkSignRequest extends BaseRequest {
    command: Extension.COMMANDS.BULKSIGN;
    data: {
        address?: string;
        txns: IndexedTransactionRequest[];
        opts?: ExtendedSignOpts;
    };
}
interface BulkSignDataResponse {
    data: {
        txBlobs: string[];
        meta: Status;
        service?: AllTransactionResponse;
    };
}
export type BulkSignResponse = BaseResponse & BulkSignDataResponse;
export interface BulkSignFullResponse extends BaseFullResponse {
    request: BulkSignRequest;
    response: BulkSignResponse;
}
export {};

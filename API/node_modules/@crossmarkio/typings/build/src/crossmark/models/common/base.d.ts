import { Extension } from '../../../extension/index';
import { Config } from '../../config';
export interface BaseRequest {
    app: typeof Config.config.title;
    type: Extension.TYPES.REQUEST;
    id: string;
}
export interface BaseResponse {
    app: typeof Config.config.title;
    type: Extension.TYPES.RESPONSE;
    id: string;
}
export interface BaseFullResponse {
    createdAt: number;
    resolvedAt?: number;
}
export interface PaymentDetails {
    address: string;
    amount: string;
    tag?: string;
    memo?: string;
}
export interface SignOpts {
    description?: string;
}
export interface ExtendedSignOpts extends SignOpts {
    service?: PaymentDetails;
}

import { BaseFullResponse, BaseRequest, BaseResponse, ExtendedSignOpts } from './common/base';
import { AllTransactionResponse, IndexedTransactionRequest } from './common/tx';
import { Status } from './common/status';
import { Extension } from '../../extension/index';
export interface BulkSignAndSubmitRequest extends BaseRequest {
    command: Extension.COMMANDS.BULK;
    data: {
        address?: string;
        txns: IndexedTransactionRequest[];
        opts?: ExtendedSignOpts;
    };
}
interface BulkSignAndSubmitDataResponse {
    data: {
        resp: AllTransactionResponse[];
        meta: Status;
        service?: AllTransactionResponse;
    };
}
export type BulkSignAndSubmitResponse = BaseResponse & BulkSignAndSubmitDataResponse;
export interface BulkSignAndSubmitFullResponse extends BaseFullResponse {
    request: BulkSignAndSubmitRequest;
    response: BaseResponse & BulkSignAndSubmitDataResponse;
}
export {};
